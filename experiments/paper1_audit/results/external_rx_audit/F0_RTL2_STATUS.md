# F0 formal status: rtl_2

The first complete F0 fold is finished for B1 and C' (seeds 0/1), initialized
from the matching X2 checkpoints and selected only by source validation. CT for
C' seeds 0/1 is also complete; TensorBoard logs are stored beside each run.

The remaining folds and B1 CT continuation are pending and can resume without
changing the locked recipe.

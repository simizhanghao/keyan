[project]
name = "di-rf-hstu"
version = "0.1.0"
description = "Domain-invariant generative RF sequence modeling for LoRa RF fingerprinting."
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "torch>=2.1",
  "tqdm>=4.66",
]

[tool.setuptools.packages.find]
where = ["src"]

